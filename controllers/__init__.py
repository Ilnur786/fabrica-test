# Common pattern name is MVC (model-view-control), and here is controllers
# In python (clearly in Django) that is MTV (model-template-view), and here is views
from controllers.client import app_client
from controllers.distribution import app_distribution
from controllers.message import app_messsage
from controllers.statistic import app_statistic